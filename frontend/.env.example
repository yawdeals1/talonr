# Base URL of the Talonr REST API, including the /api prefix. Defaults to a
# same-origin relative "/api" (see src/lib/config.ts) — override for local dev
# only if you're not running the API locally on the same host.
VITE_API_URL=http://localhost:3000/api
